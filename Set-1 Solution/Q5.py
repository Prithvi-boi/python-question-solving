# 5. Find the area of a rectangle using user input for length and width.

rec_length = int(input("length of the rectangle: "))
rec_width = int(input("Width of the rectangle: "))

area = rec_length*rec_width

print("The area of your rectangle is:", area)